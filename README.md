# Directory-Tree-Cleaner

•	Implemented a non-recursive directory tree traversal in Bash using a queue-based array structure to walk nested subdirectories
•	Automatically detects and removes zero-length files across an entire directory tree, reporting total files processed and files removed
